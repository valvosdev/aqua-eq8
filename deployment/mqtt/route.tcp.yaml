apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: mosquitto-ingress-tcp
  namespace: valvos
spec:
  entryPoints:
    - mqtttcp
  routes:
    - match: HostSNI(`mqtt.valvos.dev`)
      services:
        - name: mqtt-mosquitto
          port: 8883
  tls:
    passthrough: true 

